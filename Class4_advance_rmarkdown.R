## Here is my sample R-script in the same directory as my Rmd file
##Loading data
xvars <- 10:10
vars <- xvars^2

##print the data
xvars

print(yvars)

plot(xvars, yvars)
point(xvars, yvars,pch = 16, col = "red", cex = 1.5)